type Query {
  bookById(id: ID): Book
  getByAuthorName(authorName: String) : [Book]
  bookByUserName(bookName: String): Book
}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
}